os:
  - windows
language: node_js
node_js:
  - 10
before_script:
  - git config --global user.name "webpro"
  - git config --global user.email "lars@webpro.nl"
  - choco install git.install --force --params "/NoGitLfs /GitAndUnixToolsOnPath"
cache: npm
